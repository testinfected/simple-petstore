server.scheme: http
server.host: localhost
server.port: 8808
server.context.path:
server.webapp.path: ${project.parent.basedir}/app/src/main/webapp
server.lifecycle: simple

browser.lifecycle: lasting
## Run against selenium server by using:
#browser.lifecycle: remote
#browser.remote.url: http://127.0.0.1:4444/wd/hub
#browser.remote.capability.browserName: firefox
#browser.remote.capability.javascriptEnabled: true
#browser.remote.capability. ...

jdbc.driverClassName: ${test.jdbc.driverClassName}
jdbc.url: ${test.jdbc.url}
jdbc.username: ${test.jdbc.username}
jdbc.password: ${test.jdbc.password}
hibernate.dialect: ${test.hibernate.dialect}

migrations.path = ${project.parent.basedir}/infrastructure/src/main/scripts/migrations

timeServer.host: ${test.timeServer.host}
timeServer.port: ${test.timeServer.port}
